Hey Replit AI, I need you to seriously improve the design of my barber booking homepage.

❗ The current design looks too basic and cheap. I want it to feel like a premium SaaS product. Use Tailwind CSS and apply my brand colors:
- Dark blue (#0f172a or Tailwind `blue-900`)
- Light green (#4ade80 or Tailwind `green-400`)
- Cream (#fffdeb or use `yellow-50` as fallback)

---

✅ 1. INDEX.HTML (Tailwind + wrapper)
Make sure Tailwind is loaded:
<script src="https://cdn.tailwindcss.com"></script>

Wrap the root div:
<body class="bg-gradient-to-br from-blue-900 via-yellow-50 to-green-100 min-h-screen flex items-center justify-center font-sans">
  <div id="root" class="w-full max-w-xl rounded-3xl shadow-2xl bg-white p-8"></div>
</body>

---

✅ 2. HOME.TSX (Replace everything in `src/pages/home.tsx`)
Use this high-end looking layout:

```tsx
function Home() {
  return (
    <div className="animate-fade-in">
      <h1 className="text-4xl font-extrabold text-blue-900 text-center mb-2">SmartFlow Systems</h1>
      <p className="text-lg text-gray-600 text-center mb-6">
        Book your barber appointment in seconds.
      </p>
      <form className="space-y-5">
        <input
          type="text"
          placeholder="Full Name"
          className="w-full px-4 py-3 border rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-green-400"
        />
        <input
          type="datetime-local"
          className="w-full px-4 py-3 border rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-green-400"
        />
        <button
          type="submit"
          className="w-full bg-green-500 hover:bg-green-600 text-white font-bold py-3 rounded-xl transition duration-300"
        >
          Confirm Booking
        </button>
      </form>
    </div>
  );
}

export default Home;
✅ Optional: Add a logo, avatar, or hero image above the title for extra polish. You can use:

html
Copy
Edit
<img src="/logo.png" class="mx-auto mb-4 w-20 h-20 rounded-full shadow-md" />
✅ 3. ADD ANIMATION (optional but classy)
In your global CSS (like index.css), add:

css
Copy
Edit
@keyframes fade-in {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

.animate-fade-in {
  animation: fade-in 0.8s ease-out both;
}
Make this homepage feel like a serious software product, not a default form. Add spacing, shadows, and smooth styling.

yaml
Copy
Edit

---

💡 After this, the page will feel **branded, smoother, and professional**.

Would you like me to also design a **barbers list page** or **admin login page** with a similar premium look?






